#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b;
    cin>>a>>b;
    int diff=abs(a-b);
    int move=diff/10;
    if(diff%10!=0)
    {
        move++;
    }
    cout<<move<<endl;
    }
}
